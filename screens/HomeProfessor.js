import { View, Text, Button } from "react-native-web";
 export default function HomeProfessor() {
    return (
        <View>
            <Text>home professor não finalizada</Text>
        </View>
    );
 }